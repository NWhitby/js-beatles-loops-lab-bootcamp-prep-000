function theBeatlesPlay(arrayOfMusicians, arrayOfInstruemnts ){
  let array = [];
  for (let i = 0; i < array.length; i += 1) {
     array.push(`${arrayMusicians[i]} plays ${arrayInstruments[i]}`);
}
  }
}